import { RehearsalInterface } from '../components/rehearsal'

/**
 * Rehearsal page - wrapper for the rehearsal interface
 */
function Rehearsal() {
  return <RehearsalInterface />
}

export default Rehearsal